import { DocumentReaderItem } from '@/components/document-reader';

// All documents in DocuVault are dynamically uploaded and stored in the database / storage.
export const BASE_STAFF_DOCUMENTS: DocumentReaderItem[] = [];
